//Objetivo: Detectar clics fuera de un ref (para cerrar modales o dropdowns).

const useOnClickOutside = (ref: RefObject<HTMLElement>, callback: () => void): void => {
    // lógica con eventos globales
  };